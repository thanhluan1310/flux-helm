apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app
  namespace: apps-dev 
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
        - name: nginx
          image: nginx:1.27
